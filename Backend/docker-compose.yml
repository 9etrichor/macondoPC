version: "3.9"
services:
  backend:
    build: .
    container_name: macondo-backend
    ports:
      - "4000:4000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./prisma:/app/prisma
    command: ["node", "dist/index.js"]
